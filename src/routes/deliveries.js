const express = require("express");
const router = express.Router();
const deliveryController = require("../controllers/deliveryController"); // Adjust the path as needed

// Admin routes - assume authentication middleware is applied elsewhere if needed

module.exports = router;
